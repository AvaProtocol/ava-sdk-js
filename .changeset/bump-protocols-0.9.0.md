---
"@avaprotocol/sdk-js": minor
---

Bump `@avaprotocol/protocols` to `^0.9.0`. Picks up Arbitrum / Optimism chain IDs, `aaveV3.markets` (Ethereum Core/EtherFi/Lido/Horizon), Core reserves on the new chains, and re-exports the new Aave market/reserve types. Existing `Protocols.aaveV3.pool[chainId]` (Core) and Sepolia/Base callers are unchanged.
