{
  "commands": {
    "build": "cd ava-sdk-js && npm run build",
    "test": "cd ava-sdk-js && npx jest",
    "lint": "cd ava-sdk-js && npm run lint"
  },
  "instructions": [
    "The OpenAPI spec lives in EigenLayer-AVS `api/openapi.yaml`. Copy it with `yarn openapi-download` and regenerate types with `yarn types-gen`.",
    "For the TypeScript SDK in ava-sdk-js, use `yarn build` to build and `yarn test` to run tests."
  ]
}
